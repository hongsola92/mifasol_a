
//주석
//설명 역할, 코드로 인식되지 않는다.

int main()
{
    // 자료형 (크기 단위, byte)
    // 정수형 : char(1), short(2), int(4), long(4), long long(8)
    // 실수형 : float(4), double(8)

    //1) i라는 변수는 정수표현 방식을 따를것이다.
    //2) i라는 변수의 크기는 4byte이다.
    int i = 0;
    
    return 0;
}
